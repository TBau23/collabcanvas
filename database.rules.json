{
  "rules": {
    "sessions": {
      "$canvasId": {
        "cursors": {
          ".read": "auth != null",
          "$userId": {
            ".write": "auth.uid === $userId"
          }
        },
        "presence": {
          ".read": "auth != null",
          "$userId": {
            ".write": "auth.uid === $userId"
          }
        },
        "dragging": {
          ".read": "auth != null",
          "$shapeId": {
            ".write": "auth != null"
          }
        },
        "selections": {
          ".read": "auth != null",
          "$userId": {
            ".write": "auth.uid === $userId"
          }
        }
      }
    }
  }
}

